<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sihati_labo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SihatiLabo">
        <activity
            android:name=".scheduleDetails.ScheduleDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".createSchedulePage.CreateScheduleActivity"
            android:exported="false" />
        <activity
            android:name=".settingPage.SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".authPages.RecoverPassWordActivity"
            android:exported="false" />
        <activity
            android:name=".authPages.signupActivity"
            android:exported="false" />
        <activity
            android:name=".mainPage.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>